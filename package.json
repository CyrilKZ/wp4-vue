{
  "name": "vue-cli-webpack4-template",
  "version": "1.0.0",
  "license": "MIT",
  "description": "A vue-cli template with webpack 4 based on old webpack 3 version",
  "scripts": {
    "docs": "cd docs && gitbook serve",
    "docs:deploy": "bash ./deploy-docs.sh"
  },
  "devDependencies": {
    "vue-cli": "^2.8.1"
  }
}
